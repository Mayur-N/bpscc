# Required to enable the /admin content-editing UI. Leave both unset to disable it.
ADMIN_PASSWORD=
ADMIN_SESSION_SECRET=

# Optional: external form backend for the contact form on static hosts (e.g. a
# Formspree endpoint). Only needed when deploying the static export (GitHub Pages).
NEXT_PUBLIC_CONTACT_FORM_ENDPOINT=
